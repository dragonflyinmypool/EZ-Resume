<nav>
  <div class="footer">
    <div>
      EZ-Resume
    </div>
    <div>
      <!-- if user, show email -->
      <% if (typeof user !== "undefined") { %>
      <%= user.email %>
      <% } %>
    </div>
    <div>
      <!-- if user, show logout -->
      <% if (typeof user !== "undefined") { %>
      <a href="/logout">Logout</a>
      <% } %>
    </div>
  </div>
</nav>
<style>
  .footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 1rem;
    background-color: #f5f5f5;
    height: 3rem;
  }
</style>
</body>

</html>